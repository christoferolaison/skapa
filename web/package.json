{
  "name": "skapa-web",
  "private": true,
  "version": "0.1.3",
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next"
  },
  "dependencies": {
    "next": "^9",
    "react": "^16",
    "react-dom": "^16"
  },
  "devDependencies": {
    "@babel/preset-flow": "7.0.0"
  }
}

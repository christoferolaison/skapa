{
  "name": "skapa-web",
  "private": true,
  "version": "0.0.3-f9c5c2.1",
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next -p $PORT"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}

{
  "name": "skapa-web",
  "private": true,
  "version": "0.1.0-9a1bfa.0",
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next -p $PORT"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}

{
  "name": "skapa-web",
  "private": true,
  "version": "0.1.2",
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next -p $PORT"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@babel/preset-flow": "7.0.0"
  }
}
